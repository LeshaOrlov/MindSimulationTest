@model IEnumerable<WebApplication.Models.DataElement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ElementInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsElement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ElementInfo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsElement)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Element">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Element">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Element">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
